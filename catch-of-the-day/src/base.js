import Rebase from 're-base';

const base = Rebase.createClass({
  authDomain: "catch-of-the-day-vlad-sar.firebaseapp.com",
  databaseURL: "https://catch-of-the-day-vlad-sar.firebaseio.com",
});

export default base;